# Minimal nginx config
worker_processes 1;
error_log stderr info;

events { worker_connections 1024; }

http {
    server {
        listen 80;
        location / {
            root /usr/share/nginx/html;
            index moo.html;
        }
    }
}
